import { FC} from 'react';

interface GroupCardProps{
    title: string;
    members: string;
    maxMembers: string;
}

export const GroupCard:FC<GroupCardProps> = ({title, members, maxMembers }) =>{

    return(
        <div>
            
        </div>
    )

}

